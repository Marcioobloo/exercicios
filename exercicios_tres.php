<?php

    // questão 3.1)

    // $c = readline('Digite Celcius: ');

    // $f = (float)(($c * 9 / 5) + 32);

    // printf($c . " Celsius = " . $f . " Fahrenheit");

// =========================================================================

// questão 3.2)

    // $peso = readline('Digite o peso: ');
    // $excesso = $peso - 50;
    // $multa = $excesso * 2;

    //     if($peso < 50){
    //         echo "o peso não excede";
    //     } else {

    //     echo "O excesso será  $excesso e a multa será de $multa reais";
    //     }

// =========================================================================================

// questão 3.3)

        // $pagamento = readline("Digite quanto ganha por hora: ");
        // $horas = readline("Horas trabalhadas por mês: ");
        // $bruto = $pagamento * $horas;
        // $Renda = (11 / 100) * $bruto;
        // $inss = (8 / 100) * $bruto;
        // $sindicato = (5 / 100) * $bruto;
        // $liquido = $bruto - ($inss + $sindicato + $Renda);

        // echo "Salário bruto: ".$bruto. "\n";
        // echo "Imposto de renda: ".$Renda. "\n";
        // echo "Se paga ao INSS: ".$inss. "\n";
        // echo "se pagou ao sindicato: ".$sindicato. "\n";
        // echo "Salário liquido: ".$liquido. "\n";

?>